<Window x:Class="aXplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Axplorer SNX - Spatial Neural Plex"
        Icon="app.ico"
        Height="880" Width="1440"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="#030712"
        KeyDown="Window_KeyDown">

    <Grid Background="#030712">
        <!-- WebView2 3D Viewport & HUD -->
        <wv2:WebView2 x:Name="MainWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <!-- Loading Overlay (Visible until CoreWebView2 is ready) -->
        <Grid x:Name="LoadingSplash" Background="#030712" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Axplorer SNX" FontSize="42" FontWeight="Bold" Foreground="#38bdf8" HorizontalAlignment="Center" Margin="0,0,0,6">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#0284c7" BlurRadius="25" ShadowDepth="0" Opacity="0.8"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="SPATIAL NEURAL PLEX — INICIALIZANDO MOTOR 3D..." FontSize="12" Foreground="#94a3b8" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
